ARQUIVOS MODIFICADOS/NOVOS (copie por cima do seu repositório, mantendo os mesmos caminhos):

- composer.json
- .env.example
- app/Models/User.php
- app/Http/Middleware/EnsureNumeroIsVerified.php
- app/Actions/Fortify/CreateNewUser.php
- app/Http/Controllers/Settings/ProfileController.php
- app/Providers/FortifyServiceProvider.php
- resources/js/Pages/settings/Profile.vue
- routes/web.php
- database/migrations/2026_08_31_000000_add_tentativas_to_whatsapp_outbox.php  (NOVO)
- database/migrations/2026_08_31_000001_drop_email_verification_code_columns_from_users_table.php  (NOVO)

ARQUIVO QUE PRECISA SER APAGADO NA MÃO (não dá pra mandar a remoção de um arquivo por aqui):

- resources/js/Pages/auth/VerifyEmail.vue

Depois de aplicar tudo isso:

1. Rode `composer require resend/resend-php` (ou `composer update resend/resend-php`)
   localmente pra atualizar o composer.lock.
2. `git status` / `git diff` pra conferir.
3. `git add -A && git commit -m "..." && git push`.
4. Depois que o deploy no Railway terminar, rode `php artisan migrate`
   pra aplicar as duas migrations novas.
